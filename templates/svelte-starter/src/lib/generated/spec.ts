export const spec = {
  version: '0.1',
  name: 'tora-svelte-starter',
  description: 'Starter project generated by TORA Build.',
  entities: [
    {
      name: 'Item',
      fields: [
        { name: 'title', type: 'text', required: true },
        { name: 'created_at', type: 'datetime', required: true }
      ]
    }
  ],
  roles: [
    { name: 'admin', permissions: { Item: ['create', 'read', 'update', 'delete'] } }
  ],
  pages: [{ type: 'dashboard', name: 'Dashboard' }, { type: 'list', entity: 'Item' }],
  auth: { type: 'magic-link', self_signup: false }
} as const;

export type AppSpec = typeof spec;
