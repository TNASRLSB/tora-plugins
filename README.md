# tora-plugins

Marketplace di plugin Claude Code per TORA Cloud.

**Contenuto generato automaticamente** dal monorepo via CI — non modificare a mano.

Install:
```
/plugin marketplace add TNASRLSB/tora-plugins
/plugin install tora-deployer@tora-plugins
```
